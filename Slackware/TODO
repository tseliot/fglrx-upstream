*) Le nuove API chiedono di verificare anche la presenza di:
   - libstdc++
   - libgcc
   - fontconfig
   - freetype
   - zlib
   - gcc
   - mesa-libGL
   durante la fase di installprep. Si è scelto per ora di ignorare questa richiesta.

*) Internazionalizzare i messaggi dei doinst.sh